# Shared environment template for the TypeScript token parser API stacks (DEMOAPP001 & DEMOAPP003)
# Changing these values immediately updates the logging/config behaviour seen during batch runs.
TOKENPARSER_LOG_LEVEL=info
TOKENPARSER_LOGGER_CATEGORY=TokenParserSharedHost
